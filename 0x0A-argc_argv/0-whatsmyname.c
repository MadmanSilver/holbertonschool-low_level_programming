#include <stdio.h>
#include "holberton.h"

/**
 * main - prints the name of the function
 * @argc: size of argv
 * @argv: array of command line arguments
 *
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	UNUSED(argc);
	puts(argv[0]);
	return (0);
}
